MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=galaxy
MYSQL_PORT=3306
REDIS_PORT=6379
PMA_PORT=8081

DB_USERNAME=root
DB_PASSWORD=changeme

# HS256 needs >= 256 bits. Generate your own: openssl rand -base64 48
JWT_SECRET=changeme-generate-your-own-secret-with-openssl-rand-base64-48